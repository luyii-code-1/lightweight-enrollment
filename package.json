{
  "name": "school-club-selection",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @selection/api\" \"npm run dev -w @selection/web\"",
    "build": "npm run build -w @selection/web && npm run build -w @selection/api",
    "test": "npm run test -w @selection/api",
    "start": "npm run start -w @selection/api"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}

